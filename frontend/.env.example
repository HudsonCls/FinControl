# Em produção (Vercel), aponte para a API publicada no Render.
# Em desenvolvimento NÃO precisa definir — o Vite usa o proxy "/api" automaticamente.
VITE_API_URL=https://fincontrol-api.onrender.com/api
